#include<stdio.h>
int main()
{
        int first, second;
        printf("Please enter 2 numbers : ");
        scanf("%d%d", &first, &second);

        int sum = first + second;
        printf("%d", sum);

        return 0;
}
